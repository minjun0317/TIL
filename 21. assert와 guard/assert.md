# Assert 구문
➡️ 애플리케이션이 동작 중에 생성하는 다양한 결과값을 동적으로 확인하고 안전하게 처리하기 위해서 또 확인하며 빠르게 처리할 수 있음

| **사용 예시**
- 디버깅 모드에서만 작동한다 그래서 주로 디버깅 중 조건의 검증을 위해 사용한다
- assert( _:_:file:line: )이라는 함수를 사용

.

.

```ts
let ganadiage = -100

assert(age >= 0, "태어나면 0살 이상임")
```
