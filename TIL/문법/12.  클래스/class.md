# 클래스
➡️  클래스는 값을 저장하고 조작하는 데 사용되는 사용자 정의 데이터 타입, ```class```는 참조 타입으로 Upper Camel Case를 사용한다


| **사용 예시**
- 구조체와 비슷하다
- 구조체는 값 타입, 클래스는 참조 타입 
- 클래스는 다중 상속이 되지 않는다

.

.

.

```ts
class 클래스이름 {
    속성 및 메서드 정의
}
```
.

.

# 타입 메서드
- 재정의 불가 타입 메서드 - static
- 재정의 가능 타입 메서드 - class